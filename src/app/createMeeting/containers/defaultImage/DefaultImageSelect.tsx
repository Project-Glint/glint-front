interface DefaultImageSelectProps {
  setStep: React.Dispatch<React.SetStateAction<number>>;
}
const DefaultImageSelect = ({ setStep }: DefaultImageSelectProps) => {
  return <>123</>;
};

export default DefaultImageSelect;
